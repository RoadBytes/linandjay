import React, { Fragment } from "react"
// import { Styled } from "theme-ui"

/**
 * Change the content to add your own bio
 */

export default () => (
  <Fragment>
    Notes on life
    <br />A window to the world
  </Fragment>
)
